# AvIpEndpointTypes

**Namespace:** `gcu_hardware_service.AvIpMatrix`

Defines whether an AV-over-IP endpoint is an encoder (transmitter) or decoder (receiver).

---

## Table of Contents

- [Enum Values](#enum-values)

---

## Enum Values

```csharp
public enum AvIpEndpointTypes
```

| Value | Description |
|-------|-------------|
| `NotSet` | Default value; endpoint type has not been assigned. |
| `Encoder` | The endpoint is a transmitter (encoder). |
| `Decoder` | The endpoint is a receiver (decoder). |
